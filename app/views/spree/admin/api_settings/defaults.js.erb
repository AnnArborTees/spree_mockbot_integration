<% @settings.attributes.reject{ |k,v| ['id', 'type'].include?(k) }.each do |key, value| %>
  $input = $("input[name='<%= @settings.settings_name %>[<%= key %>]']");
  if ($input.length)
    $input.val("<%= escape_javascript value.to_s %>");
<% end %>